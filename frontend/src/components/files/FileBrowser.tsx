/**
 * FileBrowser Component (B1.2)
 *
 * NOTE (Agent-B-Academic): File browser UI is disabled.
 * The original Agent-B file workflow expects a DB/files backend.
 * For the academic workbench we use Artifacts instead.
 *
 * We keep the file to avoid import breakage, but render nothing.
 */

"use client";

// (intentionally no React import)

export function FileBrowser() {
  return null;
}
