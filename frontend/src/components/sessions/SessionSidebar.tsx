/**
 * SessionSidebar Component (B1.5)
 *
 * NOTE (Agent-B-Academic): Session management UI is disabled.
 * This project runs without a database-backed session store, so rendering
 * sessions creates noisy "Failed to load sessions" errors.
 *
 * We keep the file to avoid import breakage, but render nothing.
 */

"use client";

// (intentionally no React import)

export function SessionSidebar() {
  return null;
}
